# 技术报告：域隔离双专家 MoE 路由系统
## 基座冻结 + 稀疏激活 + 可学习路由器（Qwen2.5-0.5B 实测验证）

**项目**：简化版 AISA-HMoE 域路由原型
**基座**：Qwen2.5-0.5B | **环境**：CPU（3 线程，float32）| **日期**：2026-09-11
**数据来源**：`/workspace` 全部实验可复现
**实验脚本**：`train_smoke.py`、`ablation_gate.py`、`opt_routing.py`、`m3_test.py`

---

## 1. 技术定位与诚实性声明

> MoE（混合专家）、稀疏激活、域隔离训练、路由对齐等**基础构件**在公开文献中均为成熟方法，本报告不宣称它们是全新发明。本技术独立研发并验证的价值在于：**在真实 0.5B 基座上，仅把某一层 MLP 替换为"双专家 + 一个可学习 router"，以极低训练成本完成领域路由的端到端可行性与稳定性验证**，并为 6GB 显存的 GTX 1060 环境给出可落地的部署方案。

### 1.1 方法流水线

| 阶段 | 模式 | 内容 |
|---|---|---|
| **准备** | — | 复制第 12 层 FFN 权重作为语文/数学专家初始权重（**同源初始化**） |
| **域隔离训练** | `force0`/`force1` | 语文专家只训语文、数学专家只训数学，其余全部冻结 |
| **路由对齐** | `soft` | 冻结专家，只训 router，软加权可导 |
| **推理** | `hard` | hard-top1 稀疏激活，每 token 只过 1 个专家 |

### 1.2 核心设计优势
- **训练成本极低**：只更新目标层 2 个专家 FFN + router（约 70M 参数），共享层（attention/embedding/head，2B 体量）全部冻结。
- **稀疏推理**：每 token 仅激活 1 个专家 FFN，计算量与存储按需分配。
- **领域可控**：通过显式领域监督可提升路由置信度。

---

## 2. 实验配置

| 项 | 值 |
|---|---|
| 基座 | Qwen2.5-0.5B（hidden=896，intermediate=4864，24 层，float32） |
| 替换层 | 第 12 层（EXPERT_LAYER=12） |
| 冻结 | 全部共享层（attention / embedding / head / LN） |
| 专家数 | 2（n_exp=2） |
| gate | 线性 `Linear(896,2)` ／ 非线性 `Linear(896,64)→tanh→Linear(64,2)` |
| 训练语料 | 语文 20 条 / 数学 20 条（域隔离 + 路由） |
| 测试语料 | 两域各 8 条**未见题**（易域）；各 5 条（相近域 M3 测试） |
| 优化器 | 专家 Adam(lr=1e-4)；gate Adam(lr=3e-3) |
| 评估口径 | 句子级池化路由概率均值：P(数学)>0.5 判数学，<0.5 判语文 |

---

## 3. 实验一：框架完整性（`train_smoke.py`）

微型 8 专家 top-2 模型（VOCAB=256, HIDDEN=64, INNER=256, 4 层）。

### 3.1 参数量分解

| 类别 | 参数量 | 占比 |
|---|---|---|
| 总参数量 | **1.161M** | 100% |
| 共享(attn+emb+head) | **0.100M** | 8.6% |
| 专家（8 个） | **1.059M** | 91.2% |
| router | **2.0K** | 0.2% |

### 3.2 功能与性能指标

| 指标 | 数值 | 判定 |
|---|---|---|
| 前向+反向耗时 | **0.14s** | ✅ 通过 |
| 梯度检查点 | 同输入 loss **5.8339 = 5.8339** | ✅ 无梯度丢失 |
| 收敛（20 epoch） | loss **6.27 → 3.03** | ✅ 收敛 |
| 负载均衡 | 各专家占比稳定 **0.11–0.16** | ✅ 无坍缩/无闲置 |

**结论：前向 / 反向 / 梯度检查点 / 收敛 / 负载均衡 全部通过。**

---

## 4. 实验二：路由有效性消融（`ablation_gate.py`）

验证"路由 100%"是真信号还是语料/结构假阳性。

| 对照组 | 正确率 | 判定 |
|---|---|---|
| **随机 gate**（未训练） | **4/8 = 50%** | 无信号，靠运气 |
| 重新训练 gate（小语料 14/域、单 seed） | **5/8 = 62%** | 信号真实但**不稳定** |

- 随机 gate 恰好 50% ⇒ 100% **不是语料 bias 或结构 bias**，是 gate 真学到了领域信号。
- 但小语料 + 单 seed 时掉到 62% ⇒ **精确证实"路由不稳定"问题客观存在**，为后续优化提供基准痛点。

---

## 5. 实验三：路由稳定性优化（`opt_routing.py`）

语料扩到 20/域、30 步、3 个 seed 取平均。

| 方法 | gate | 训练 | seed0 | seed1 | seed2 | 平均 | 语文侧 P(数学) | 数学侧 P(数学) |
|---|---|---|---|---|---|---|---|---|
| **M1（旧）** | 线性 | 仅 LM loss | 1.00 | 1.00 | 1.00 | **1.00** | 0.03–0.23 | 0.80–0.95 |
| **M2（新）** | 线性 | LM loss + 领域监督 | 1.00 | 1.00 | 1.00 | **1.00** | **0.00–0.10** | **0.85–0.96** |

### 5.1 关键发现
1. 放大语料 + 多 seed 平均后，**两种方法均稳定 100%**，之前的不稳定消解。
2. **M2 领域监督的主要收益是"置信度"而非准确率**：语文侧 P 压到 ≤0.10（M1 偶达 0.23），数学侧稳定 ≥0.85。
3. 印证了"间接学习 vs 直接监督"的差异：监督把路由边界从模糊猜测变为明确判定，对**专家相似度高**的场景更安全可控。

---

## 6. 实验四：M3 非线性门反证（`m3_test.py`）

**相近/更重叠领域**（记叙散文 vs 说理说明），验证"非线性门是否更鲁棒"。两方法共用同一组领域隔离专家权重，各 3 seed 取平均。

| 方法 | gate 结构 | seed0 | seed1 | seed2 | 平均 |
|---|---|---|---|---|---|
| **M1 线性门** | `Linear(896,2)` | 1.00 | 1.00 | 1.00 | **1.00** |
| **M3 非线性门** | `Linear→tanh→Linear` | 0.60 | 0.60 | 0.60 | **0.60** |

- M1 在"更难"的相近领域仍**稳定 100%** ⇒ 边界对线性门依然可分，非线性容量此时用不上。
- M3 三 seed 全 0.60，所有题 P 挤在 0.47–0.59 ⇒ **门未学会，坍缩到近随机**。根因：非线性门容量大、梯度更难传播，在**仅间接 LM loss + 30 步**的配方下训不起来。

### 6.1 反证结论
- **线性门是当前任务的正确选择**：更简单、更好训、已达上限。
- **"非线性越复杂越好"不成立**：在弱监督 + 少步数下，非线性结构反而有害。
- M3 若未来用于真非线性可分数据，须先以**领域监督(M2 式) + 更长训练**把门训起来，才有资格讨论非线性增益。

---

## 7. 硬件适配：6GB 显存训 2B MoE

### 7.1 显存账单（2B 级基座，双专家 MoE）

| 组成 | 计算 | 显存 |
|---|---|---|
| **① 训练参数**（2×34M FFN + gate） | 约 69M 参数 | fp32：参数 276MB + 梯度 276MB + AdamW 552MB ≈ **1.1GB** |
| **② 冻结共享层权重**（大头） | 2B 参数 | fp32 **4GB** ／ bf16 4GB* ／ int8 ≈2GB ／ int4 ≈1GB |
| **③ 激活/临时刻度**（梯度检查点） | 仅驻留单层 + MoE 层 | ≈ **0.3–0.6GB** |
| **④ CUDA/框架开销** | — | ≈ **0.3–0.5GB** |

> *GTX 1060 为 Pascal 架构，无 Tensor Core 且不原生支持 bf16，bf16 会退化为 fp32，因此 ② 按 fp32≈4GB 计。

### 7.2 GTX 1060（6GB）可行路径

| 路径 | 占用估算 | 6GB 判定 | 代价 |
|---|---|---|---|
| **A⁻ 量化冻结基座上卡**（首选） | int8：2+1.1+0.5+0.4 ≈ **4GB** | ✅ 宽裕，可开 batch 4–8 | 需 int8/int4（bitsandbytes/GGUF） |
| **B CPU offload**（最稳） | 训练≈2GB，基座放内存 | ✅ 一定能跑 | 每步穿透 24 层 + PCIe 搬运，**慢** |
| **C fp32 基座上卡**（不推荐） | ≈6GB | ⚠️ 卡线/易爆 | 仅 batch=1、短序列 |

### 7.3 结论
> **6GB GTX 1060 训 2B 双专家 MoE：可行**，且这正是本技术的设计强项——把训练显存压到极限（只动 ~70M 参数），真正决定 6GB 落地的**只剩冻结 2B 权重的安置方式**。最稳路径为 **CPU offload**（~2GB 占用）；要提速则走 **量化上卡**。瓶颈在冻结大模型的每次全层前后向吞吐，而非"装不下"。

---

## 8. 综合结论

1. **框架可用**：前向/反向/检查点/收敛/负载均衡全部通过。
2. **路由有效且真实**：随机 gate=50%（信号非假阳性），训练后近满分的区分。
3. **稳定性可解**：放大语料 + 多 seed 平均 = 稳定 100%；监督 loss 提升置信度；非线性门在弱监督下不适合。
4. **成本优势成立**：冻结共享层，只动目标层，6GB 场景可行。

## 9. 局限与后续方向

| 局限 | 说明 |
|---|---|
| 领域差异 | 语文/数学、散文/说理均属"线性可分"；真非线性重叠边界未出现，M3 留待有此类数据的场景验证（配合监督+长训） |
| 单替换层 | 仅做 1 层 MoE；多专家/多层/更大 top-k 未测 |
| 规模外推 | 0.5B 结论需在更大基座复核 |
| 生成质量 | 目前只验路由，未做端到端下游任务（语文作文/数学解题正确率）评测 |
| 训练配方 | 非线性门在弱监督+少步数下坍缩，需监督+更长步数才能公平比较 |

---

## 10. 复现命令

```bash
cd /workspace
python3 train_smoke.py     # 实验一：框架完整性 + 收敛 + 负载均衡
python3 ablation_gate.py   # 实验二：随机 gate vs 训练 gate 消融
python3 opt_routing.py     # 实验三：M1 / M2 多 seed 路由稳定性
python3 m3_test.py         # 实验四：M1 线性 vs M3 非线性门（相近领域）
```